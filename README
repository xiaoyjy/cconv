======= INSTALL =======
Make Makefile.PL
./configure
make
sudo make install

* NOTICE :
You need install cconv-0.5.1 or later first, and
make sure libcconv.so can be found by ldconfig and cconv.h include path in CFLAGS.

